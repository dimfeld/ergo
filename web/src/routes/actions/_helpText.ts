export const templateInputs = `A task that triggers an action can provide the template inputs defined here. These values can then be used in the executor template.`;

export const executorTemplate = `This section defines how the executor template is filled in. It can include hardcoded values or template values
  from accounts and the template inputs.`;
